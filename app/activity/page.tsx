import { ActivityFeed } from "@/components/ActivityFeed";

export const metadata = { title: "Request Activity · Codex Subscription Usage" };

export default function ActivityPage() {
  return <ActivityFeed />;
}
